# PostgreSQL for the shared team database (recommended)
# DATABASE_URL=postgresql+psycopg2://username:password@localhost:5432/citymemory
#
# When DATABASE_URL is omitted, CITYMEMORY starts with a local SQLite database.
